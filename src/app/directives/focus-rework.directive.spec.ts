import { FocusReworkDirective } from './focus-rework.directive';

describe('FocusReworkDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusReworkDirective();
    expect(directive).toBeTruthy();
  });
});
