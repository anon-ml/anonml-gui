import { KeyControlDirective } from './key-control.directive';

describe('KeyControlDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyControlDirective();
    expect(directive).toBeTruthy();
  });
});
