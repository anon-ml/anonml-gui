import { FocusMainDirective } from './focus-main.directive';

describe('FocusMainDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusMainDirective();
    expect(directive).toBeTruthy();
  });
});
