import { HighlightAnonymizationPipe } from './highlight-anonymization.pipe';

describe('HighlightAnonymizationPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightAnonymizationPipe();
    expect(pipe).toBeTruthy();
  });
});
